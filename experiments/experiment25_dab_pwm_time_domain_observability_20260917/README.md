# EXP-025 — DAB PWM 时域观测与逐匝图参数可辨识性

本实验把 EXP-024 的理想复导纳观测推进到有限上升沿、死区衰减、相移和电压比变化的 DAB 方波窗口。脚本合成并采样 `v1/i1/v2/i2`，再从 FFT 谐波重构两端口导纳。

它是“开关波形 + 线性逐匝网络”的协同仿真，不是含 MOSFET 非线性、Coss 和换流过程的完整器件级仿真。

运行：

```powershell
python run_exp25.py
```

比较三种观测：四个独立 DAB 运行窗口、四个退化重复窗口和单窗口。目标是验证运行窗口的端口激励秩，而不是重新做离线选频。
